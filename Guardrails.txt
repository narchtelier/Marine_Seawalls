Don't push to GitHub unless I tell you to.
Don't pull from GitHub unless I tell you to.
Always push and pull with Git from my account narchtelier with the email narchtelier@gmail.com.
Be brief, concise and always avoid long summaries and filler words in your responses to save token usage.
Make sure the document title matches the repository name so the browser always shows the repo name.
On the first git push make sure the default main branch is named main, do not change name to anything else.
Give each git committed file or folder a unique description based on what the commit is about.
Create an initial README.md which give a paragraph summary of the project as a whole as well as a feature list, getting started, and controls at the first push commit to GitHub.
The README.mg header should always be the same as the repository name.
Update the README.md before each push commit to GitHub to include any major new features, getting started, or controls, don't update the README.md with minor changes.
Don't keep track of git commits after each instruction or git diff cacheing, only check changes when merging and pushing to git.
Don't compact the context unless I tell you to. 